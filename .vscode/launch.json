{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Solver",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/solver",
      "args": ["--keys", "./config/local/keys.json", "--log-level", "debug"]
    }
  ]
}
